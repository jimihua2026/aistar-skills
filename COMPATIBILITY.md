# Compatibility

## 定位

本仓库使用以 `SKILL.md` 为入口、以相对路径组织参考资料和模板的 Agent Skills 结构。Codex 是当前已验证宿主；其他平台的安装目录、触发语法和工具接口由各平台决定。

## 能力矩阵

| 能力 | 星创导演 | 镜头解构 | 缺失时的处理 |
| --- | --- | --- | --- |
| 读取 Markdown 与相对路径资源 | 必需 | 必需 | 无法完整运行 |
| 写入工作区文件 | 必需 | 必需 | 改为聊天内交付，明确未落盘 |
| 读取文档、表格与图片 | 按输入需要 | 读取视频帧时必需 | 请求用户转换输入或仅完成可验证部分 |
| 音频识别与语音转写 | 按输入需要 | 仅在用户要求音频分析时需要 | 跳过音频部分并说明限制 |
| 本地命令执行 | 非必需 | 必需 | 镜头解构停止视频处理并报告缺失能力 |
| FFmpeg 与 FFprobe | 非必需 | 必需 | 经用户授权后安装，或让用户提供关键帧和元数据 |
| 自动 Skill 发现与调用 | 可选 | 可选 | 手动加载 `SKILL.md` |
| 持久化个人文件 | 用于 Personal Layer | 非必需 | 当次会话仍可运行，但不保证跨会话积累 |

## 宿主适配原则

1. 将 Skill 中的“读取文件、写入文件、执行命令、查看图片”等能力映射到宿主 Agent 提供的等价工具，不依赖固定工具名称。
2. 所有相对路径均以当前 Skill 目录为基准解析。
3. 宿主缺少某项非核心能力时，只降级对应部分，不伪造观察、工具结果或已写入文件。
4. 安装软件、访问网络、覆盖文件或执行其他外部修改前，遵循宿主平台的权限机制并取得必要授权。
5. 平台兼容状态只有在对应版本完成真实测试后才能标记为“已验证”；未经测试的平台统一标记为“理论兼容”。

## 当前验证状态

| 宿主 | 状态 | 说明 |
| --- | --- | --- |
| Codex | 已验证 | 两个 Skill 均通过格式校验；实际运行能力仍取决于会话权限和本地依赖。 |
| 其他支持 Agent Skills 的宿主 | 理论兼容 | 核心结构可移植，安装与工具映射待按平台验证。 |
| 不支持 Agent Skills 的宿主 | 手动兼容 | 可把 `SKILL.md` 作为项目或系统指令加载，并同时提供其引用资源。 |
